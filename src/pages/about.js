import React from 'react'
import Layout from '../components/layout';
import Intro from '../components/intro';

const AboutPage = (props) => {
    return (
        <Layout>
            <Intro title="About" text="Being a freelancer sounds maybe more beautiful then it is. Being a freelancer sounds maybe more beautiful then it is. Being a freelancer sounds maybe more beautiful then it is." />
        </Layout>
    )
}

export default AboutPage;
